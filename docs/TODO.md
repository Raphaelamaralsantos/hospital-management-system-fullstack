# TODO

## Checklist
- [x] Identificar que a página que salva no banco é `agendar.html` (usa `fetch` para `server/api/agendamentos.php`).
- [x] Confirmar que a home (`index.html`/`Tela-home.html`) não envia dados para o backend ao agendar.
- [ ] Implementar correção: conectar o fluxo do formulário que agenda a partir da home ao backend PHP.
- [ ] Testar: agendamento deve inserir na tabela `agendamentos` e exibir retorno/erro.

