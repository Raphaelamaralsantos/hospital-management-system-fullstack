# TODO-db-xampp

- [ ] Corrigir formulário de agendamento na home (`consulta.html`) para enviar dados ao backend `server/api/agendamentos.php`.
- [ ] Ajustar campos: `nome`, `email`, `especialidade`, `data`, `hora`, `observacoes`.
- [ ] Mostrar feedback do backend (sucesso/erro) em vez de apenas `alert()`.
- [ ] Testar: garantir que `INSERT INTO agendamentos` acontece e que a tabela existe no banco `hospital`.

